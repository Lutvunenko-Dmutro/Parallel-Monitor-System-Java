# 🛠 Технічна документація: Збірка проекту (Build Process)

У цьому документі описано процес ручної компіляції та пакування проекту у виконуваний файл `.exe`. 
Ми не використовували сторонніх збирачів або зовнішніх програм. Використано виключно **нативні інструменти JDK та Windows**.

## Етап 1: Створення "Fat JAR" (Uber-JAR)

Для того щоб програма була портативною (один файл), зовнішню бібліотеку `FlatLaf` було інтегровано всередину основного архіву вручну.

**Виконані кроки (Command Line):**

1.  **Підготовка:**
    Бібліотеку `flatlaf-3.6.2.jar` було розпаковано, видалено папку `META-INF` та файл `module-info.class` для уникнення конфліктів.
2.  **Пакування:**
    Усі класи (код програми + класи бібліотеки) зібрані в один архів командою:
    ```powershell
    jar cfe Lab4.jar MainClass .
    ```

---

## Етап 2: Створення EXE (Windows IExpress)

Для створення виконавчого файлу `.exe` використано вбудований інструмент Windows — **IExpress Wizard**.

**Покроковий алгоритм створення:**

1.  **Запуск майстра:**
    * Натиснути `Win` + `R`.
    * Ввести команду `iexpress` і натиснути **Enter**.

2.  **Налаштування (Wizard Steps):**
    * **Welcome:** Обрати `Create new Self Extraction Directive file`. -> *Next*.
    * **Package purpose:** Обрати `Extract files and run an installation command`. -> *Next*.
    * **Package title:** Ввести назву проекту: `Lab4`. -> *Next*.
    * **Confirmation prompt:** Обрати `No prompt`. -> *Next*.
    * **License agreement:** Обрати `Do not display a license`. -> *Next*.
    * **Packaged files:**
        * Натиснути **Add**.
        * Обрати підготовлений файл `Lab4.jar`. -> *Next*.
    * **Install Program to Launch (⚠️ Важливо):**
        * У полі "Install Program" вписати команду запуску:
          ```cmd
          java -jar Lab4.jar
          ```
        * Поле "Post Install" залишити пустим. -> *Next*.
    * **Show window:** Обрати `Hidden` (для приховання консольного вікна). -> *Next*.
    * **Finished message:** Обрати `No message`. -> *Next*.
    * **Package Name:**
        * Натиснути **Browse**.
        * Вказати шлях та ім'я файлу: `Lab4.exe`. -> *Next*.
    * **Configure restart:** Обрати `No restart`. -> *Next*.
    * **Save Self Extraction Directive:** Обрати `Don't save`. -> *Next*.
    * **Create package:** Натиснути *Next* для початку збірки -> *Finish*.

---

## ✅ Результат

Отримано файл `Lab4.exe`, який:
1.  **Автономний:** Містить у собі код програми та необхідні бібліотеки.
2.  **Чистий:** Не потребує встановлення сторонніх лаунчерів.
3.  **Нативний:** Використовує встановлену у системі Java Virtual Machine.
